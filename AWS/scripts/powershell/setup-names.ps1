# Should be unique to this AWS account [a-zA-Z][-a-zA-Z0-9]*
$env:APP_NAME="expressbackend"

# Template URL
$env:TEMPLATE_URL=

# Root password for the database
$env:DB_ROOT_PASSWORD="CHANGE_THIS_PASSWORD"

# Subdomain to use
$env:SUBDOMAIN_NAME=

# Get this values from AWS. Use any two subnets for now.
$env:VPC_ID=
$env:SUBNET_ID_1=
$env:SUBNET_ID_2=