# docker network (should be different from other docker networks in your system)
export DOCKER_NETWORK=express_network

# docker image names (should be different from other docker images in your system)
export TAG_NAME=express_server
export DEV_TAG_NAME=express_server_dev
export MYSQL_HOST=mysql_express

# DB config
export MYSQL_DB_NAME=express_db
export MYSQL_ROOTPASSWORD=testrootpassword
export MYSQL_USER=backend_user
export MYSQL_PASSWORD=testpassword